<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vacation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vacation">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoadingActivity" android:label="로딩화면"/>
        <activity android:name=".keyword" android:label="키워드 "/>
        <activity android:name=".exhibition" android:label="전시회 "/>
        <activity android:name=".choose" android:label="월드컵 "/>
        <activity android:name=".InformationActivity" android:label="정보화"/>
        <activity android:name=".date" android:label="달력"/>
    </application>

</manifest>